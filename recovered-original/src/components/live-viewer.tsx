"use client";
import { useEffect, useState } from "react";
type LiveData = { active:boolean; match:{ runs:number; wickets:number; balls:number; teamAId:number; teamBId:number; battingTeamId:number; venue:string }; tournament?:{name:string}; teams:{id:number;name:string;shortName:string;logoUrl?:string|null}[]; error?:string };
export function LiveViewer(){ const [data,setData]=useState<LiveData|null>(null); const [frameUrl,setFrameUrl]=useState(""); const [hasFrame,setHasFrame]=useState(false); const token=typeof window==="undefined"?"":new URLSearchParams(location.search).get("token")??""; useEffect(()=>{ const load=()=>void fetch(`/api/live?token=${encodeURIComponent(token)}`,{cache:"no-store"}).then(r=>r.json()).then(setData); load(); const scoreTimer=setInterval(load,3000); const frameTimer=setInterval(()=>setFrameUrl(`/api/live/frame?token=${encodeURIComponent(token)}&v=${Date.now()}`),700); const initialFrameTimer=setTimeout(()=>setFrameUrl(`/api/live/frame?token=${encodeURIComponent(token)}&v=${Date.now()}`),0); return()=>{clearTimeout(initialFrameTimer);clearInterval(scoreTimer);clearInterval(frameTimer);}; },[token]); if(!data)return <main className="viewer-page"><p>Connecting to Core Cricket Live…</p></main>; if(data.error)return <main className="viewer-page"><h1>Live stream unavailable</h1><p>{data.error}</p></main>; const batting=data.teams.find(t=>t.id===data.match.battingTeamId); const other=data.teams.find(t=>t.id!==data.match.battingTeamId); return <main className="viewer-page"><header><b>CORE CRICKET</b><span><i/> {data.active?"LIVE":"ENDED"}</span></header><section className="viewer-video">{frameUrl&&<img className="live-camera-frame" src={frameUrl} alt="Live cricket camera" onLoad={()=>setHasFrame(true)}/>} {!hasFrame&&<div className="camera-waiting"><div className="camera-icon">▣</div><h2>Connecting to live camera…</h2><p>Keep this page open. Video begins when the broadcaster&apos;s first camera frame arrives.</p></div>}<div className="viewer-score"><span>{data.tournament?.name??"Live cricket"}</span><strong>{batting?.shortName} {data.match.runs}/{data.match.wickets}</strong><b>{Math.floor(data.match.balls/6)}.{data.match.balls%6} OVERS</b><em>vs {other?.name}</em></div></section><footer>{data.match.venue} · Camera and score refresh automatically</footer></main> }
