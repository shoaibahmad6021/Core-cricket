export type Role = "site_admin"|"tournament_creator"|"team_admin"|"captain"|"scorer"|"viewer";
export type PlayerRole = "batter"|"bowler"|"all_rounder"|"wicketkeeper";
export type Team = { id:string; name:string; shortName:string; city:string; color:string; adminUserId?:string };
export type Player = { id:string; name:string; teamId:string; role:PlayerRole; battingStyle:"right"|"left"; runs:number; balls:number; dismissals:number; fours:number; sixes:number; wickets:number; ballsBowled:number; runsConceded:number; matches:number };
export type Group = { id:string; name:string; teamIds:string[] };
export type Tournament = { id:string; name:string; format:string; venue:string; startDate:string; creatorUserId:string; groups:Group[]; mvpOverridePlayerId?:string };
export type Delivery = { id:string; runsBat:number; extras:number; extraType?:"WD"|"NB"|"B"|"LB"; legal:boolean; wicket:boolean; batterId?:string; bowlerId?:string };
export type Innings = { teamId:string; runs:number; wickets:number; legalBalls:number };
export type Match = { id:string; tournamentId?:string; groupId?:string; teamAId:string; teamBId:string; overs:number; venue:string; status:"scheduled"|"live"|"completed"; winnerTeamId?:string; innings:Innings[]; deliveries:Delivery[]; mvpOverridePlayerId?:string; scorerUserIds:string[] };
export type User = { id:string; phone:string; name:string; role:Role; password:string };
export type AppState = { users:User[]; currentUserId?:string; teams:Team[]; players:Player[]; tournaments:Tournament[]; matches:Match[] };
